import 'package:flutter/services.dart';

class Constants {
  static const iosChannel = "nan.native_biometrics";
  static const verify = "verify";
  static const channel = MethodChannel(Constants.iosChannel);
}
